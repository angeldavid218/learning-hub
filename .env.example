# Supabase project settings -> API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Supabase project settings -> Database -> Connection string (transaction pooler)
DATABASE_URL=

# Optional local brand routing (JSON Host -> brand id)
# BRAND_HOST_MAP={"localhost:3000":"agentlabs","timerich.local:3000":"timerich"}

# Optional: promote an existing local user to brand_admins on npm run db:seed
# (sign up once first so a profiles row exists — see docs/onboarding.md)
# SEED_ADMIN_USER_ID=
# SEED_ADMIN_EMAIL=
# SEED_ADMIN_BRAND_IDS=agentlabs,timerich
